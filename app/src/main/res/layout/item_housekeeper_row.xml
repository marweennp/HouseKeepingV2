<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/white">

    <!-- Photo -->
    <android.support.v7.widget.AppCompatImageView
        android:id="@+id/img_housekeeper_row_photo"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_alignParentStart="true"
        app:srcCompat="@drawable/fm" />

    <!-- Name -->
    <android.support.v7.widget.AppCompatTextView
        android:id="@+id/tv_housekeeper_row_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:layout_marginEnd="8dp"
        android:layout_centerVertical="true"
        android:layout_toEndOf="@id/img_housekeeper_row_photo"
        android:layout_toStartOf="@id/rl_housekeeper_row_rooms"
        android:ellipsize="end"
        android:maxLines="2"
        android:text=""
        android:textColor="@color/colorSecondaryDark"
        android:textSize="18sp"
        android:textStyle="bold" />

    <!-- Rooms -->
    <RelativeLayout
        android:id="@+id/rl_housekeeper_row_rooms"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_centerVertical="true"
        android:layout_alignParentEnd="true"
        android:layout_marginEnd="8dp"
        android:background="@drawable/notification_badge">

        <android.support.v7.widget.AppCompatTextView
            android:id="@+id/tv_housekeeper_row_rooms"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:layout_marginBottom="2dp"
            android:text="10"
            android:textColor="@color/white"
            android:textSize="14sp"
            android:textStyle="bold" />

    </RelativeLayout>

</RelativeLayout>
